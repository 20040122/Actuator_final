# 项目 TODO（按优先级拆分）

## P0 里程碑（必须优先完成，影响主流程正确性）

### Epic P0-1：打通真实分布式通信闭环（发送 + 接收 + 处理）

- [ ] P0-1.1 定义消息处理入口与路由表（按 `MessageType` 分发处理函数）
- [ ] P0-1.2 完成 `BATCH_TASK_ASSIGN` 反序列化并接入卫星侧执行入口
- [ ] P0-1.3 实现 `BATCH_TASK_ASSIGN_ACK` 回传与协调器侧处理
- [ ] P0-1.4 实现 `TASK_PROGRESS` / `TASK_COMPLETE` 上报链路
- [ ] P0-1.5 增加端到端集成测试（协调器->卫星->回执）
- [ ] P0-1.6 验收：禁用本地直调执行器后，仍可完成一次完整任务执行

### Epic P0-2：修复分布式信号量持有者建模错误

- [ ] P0-2.1 将 holder 主键从 `local_node_id_` 改为 `request_id + task_id + owner_id`
- [ ] P0-2.2 `acquire()` 返回并记录 grant token，建立请求与持有映射
- [ ] P0-2.3 `release()` 支持按 token/请求精确释放，禁止“整节点全释放”
- [ ] P0-2.4 增加并发场景单元测试（同节点多任务竞争同一信号量）
- [ ] P0-2.5 验收：日志中释放 permit 数量与申请数量逐请求一致

### Epic P0-3：行为树命令改为可验证执行结果

- [ ] P0-3.1 为 `ATTITUDE_ADJUST` 增加真实处理器与失败条件
- [ ] P0-3.2 为 `PAYLOAD_CONFIG` 增加真实处理器与状态输出
- [ ] P0-3.3 为 `PAYLOAD_CAPTURE` 增加真实处理器与产物变量（数据量/状态）
- [ ] P0-3.4 为 `DATA_STORE` 增加容量校验与失败返回
- [ ] P0-3.5 为 `EMIT_EVENT` 增加事件落盘/内存总线发布
- [ ] P0-3.6 未注册命令统一返回失败（含错误码与错误信息）
- [ ] P0-3.7 验收：任一命令失败可触发回滚且能定位失败节点

### Epic P0-4：自动化测试最小可用集

- [ ] P0-4.1 约束表达式测试：比较/逻辑组合/非法输入
- [ ] P0-4.2 信号量测试：申请/释放/超时/优先级队列
- [ ] P0-4.3 行为树执行测试：成功路径/条件失败/命令失败回滚
- [ ] P0-4.4 通信链路集成测试：任务下发->执行->回执
- [ ] P0-4.5 接入 CI（构建 + 单测 + 集成测试）
- [ ] P0-4.6 验收：核心路径改动可被测试阻断回归

## P1 里程碑（高优先级，提升可用性与鲁棒性）

### Epic P1-1：统一任务分发路径

- [ ] P1-1.1 引入 `execution_mode = local | distributed` 配置
- [ ] P1-1.2 抽象统一接口：`dispatchTasks()`，内部按模式选择实现
- [ ] P1-1.3 清理重复逻辑（`distributeAllTasks()` 与 `executeTasksForSatellite()`）
- [ ] P1-1.4 验收：两种模式均可独立跑通并有清晰日志标识

### Epic P1-2：通信可靠性增强

- [ ] P1-2.1 为关键消息增加 ACK 等待与超时机制
- [ ] P1-2.2 增加可配置重试策略（重试次数/退避间隔）
- [ ] P1-2.3 基于 `message_id/sequence_id` 做幂等去重
- [ ] P1-2.4 增加 checksum 校验与坏包处理
- [ ] P1-2.5 验收：模拟丢包/延迟时任务状态一致

### Epic P1-3：同步屏障与死锁策略落地

- [ ] P1-3.1 实现 barrier 状态机（等待/超时/释放）
- [ ] P1-3.2 将 `deadlock_detection` 与 `deadlock_resolution` 接入运行时
- [ ] P1-3.3 增加策略动作（中止低优先级、重试、降级）
- [ ] P1-3.4 验收：多节点同步点在超时/缺席场景行为可预期

### Epic P1-4：状态与事件模型强化

- [ ] P1-4.1 定义 `TaskStatus` 合法迁移表
- [ ] P1-4.2 在状态更新处加迁移校验与错误日志
- [ ] P1-4.3 统一事件日志结构（任务ID、节点ID、时间戳、状态）
- [ ] P1-4.4 验收：任务全生命周期可完整回放

## P2 里程碑（中优先级，工程化与扩展）

### Epic P2-1：约束表达式引擎增强

- [ ] P2-1.1 支持更完整语法（运算符优先级、括号错误定位）
- [ ] P2-1.2 增加 token 级错误信息（位置/上下文）
- [ ] P2-1.3 明确时间/数值/字符串比较类型策略
- [ ] P2-1.4 验收：复杂表达式可稳定解析并返回可读错误

### Epic P2-2：配置与输入校验

- [ ] P2-2.1 定义 `global/schedule/behavior` JSON schema
- [ ] P2-2.2 启动阶段增加 schema 校验并阻断非法配置
- [ ] P2-2.3 增加配置版本号与向后兼容策略
- [ ] P2-2.4 验收：配置错误可在初始化阶段精确定位字段

### Epic P2-3：可观测性建设

- [ ] P2-3.1 输出结构化日志（JSON）
- [ ] P2-3.2 暴露关键指标（任务时延、失败率、资源占用）
- [ ] P2-3.3 提供指标采集出口（文件/Prometheus）
- [ ] P2-3.4 验收：可按卫星、任务、资源维度检索运行状态

### Epic P2-4：文档与示例完善

- [ ] P2-4.1 更新 README（架构、启动、配置、调试）
- [ ] P2-4.2 增加示例：单星成功、多星竞争、失败回滚
- [ ] P2-4.3 增加故障排查文档（常见报错与处理）
- [ ] P2-4.4 验收：新开发者 10 分钟内可跑通最小示例
